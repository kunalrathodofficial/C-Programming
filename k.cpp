#include<iostream>
using namespace std;
int main(){
	string s;
	cin>>s;
	int freq[26];
	for(int i=0;i<26;i++){
		freq[i]=0;
	}
	int i=0;
	switch :
		
		case A:
		freq[freq[i]-'A']++;
	    case T:
		freq[freq[i]-'T']++;
		case G:
		freq[freq[i]-'G']++;
		case C:
		freq[freq[i]-'C']++;
		i++;
		int max=0;
		for(int i=0;i<26;i++){
		maxF=max(max,freq[i]);
	}
		cout<<maxF;
	return 0;
}
